/**
 * Types UI réutilisables (composants partagés).
 */

/** Élément sélectionnable dans une liste (single ou multi). */
export type SelectableItem = {
  id: string;
  initial: string;
  label: string;
};
