/**
 * Constantes Supabase utilisables côté client (pas d'import de server.ts).
 */

/** ID de la conversation "room" tant qu'il n'y a pas de table conversations. */
export const ROOM_CONVERSATION_ID = "room";
