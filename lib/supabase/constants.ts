/**
 * Constantes Supabase partagées (client + server).
 * Source unique pour éviter la duplication (DRY).
 */

/** ID de la conversation "room" (une seule pour tout le monde). */
export const ROOM_CONVERSATION_ID = "room";

/** Nom du bucket Storage pour les médias des messages (image/vidéo). */
export const MEDIA_BUCKET = "media";
