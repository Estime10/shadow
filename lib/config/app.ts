/**
 * Nom de l'application (affichage header, titre, etc.).
 */
export const APP_NAME = "Ghost Riders" as const;

/**
 * Nom court (notifications, fallback).
 */
export const APP_SHORT_NAME = "Ghost" as const;
