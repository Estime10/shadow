/**
 * Constantes de layout partagées (nav, safe area, padding pages).
 */

/** Hauteur de la barre de navigation en rem (équivalent Tailwind pb-20). */
export const NAV_BOTTOM_REM = 5;

/** Hauteur de la barre de navigation en px (pour calculs JS). */
export const NAV_BOTTOM_PX = NAV_BOTTOM_REM * 16;
